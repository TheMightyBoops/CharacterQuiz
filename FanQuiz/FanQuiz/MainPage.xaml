<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FanQuiz"
             x:Class="FanQuiz.MainPage"
             >

    <StackLayout BackgroundColor="Black">
        <!-- Place new controls here -->
        <Label x:Name="questionLabel" Text="{Binding BaseQuestion}" TextColor="White"></Label>
        <Button x:Name="A" Text="{Binding AnswerA}" Clicked="AnswerA" BackgroundColor="White"></Button>
        <Button x:Name="B" Text="{Binding AnswerB}" Clicked="AnswerB" BackgroundColor="White"></Button>
        <Button x:Name="C" Text="{Binding AnswerC}" Clicked="AnswerC" BackgroundColor="White"></Button>
        <Button x:Name="D" Text="{Binding AnswerD}" Clicked="AnswerD" BackgroundColor="White"></Button>
        <Label x:Name="Character"></Label>
        <Image x:Name="Complete"></Image>
    </StackLayout>

</ContentPage>
